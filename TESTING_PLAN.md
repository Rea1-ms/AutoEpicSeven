# 测试整理与验收计划

基线日期：2026-09-26。状态只使用“未开始、进行中、已验证”。这里记录已经取得的结果和尚待核对的验收项；一项测试通过，不等于整个游戏流程已覆盖。

## 目标与边界

本机和 GitHub Actions（云端自动化服务）从同一个入口运行同一批离线用例、截图和判断标准。默认测试只读取仓库中的样本，以模拟设备记录操作，不连接模拟器，不使用真实账号配置。实机调试仍由人手动启动。首轮只迁移肉鸽；其他业务和 Alasio（另一框架）相关测试只盘点。

## 迁移前基线与文件约定

| 项目 | 基线或约定 |
| --- | --- |
| 历史脚本 | 主检出目录被忽略的 test（历史测试）目录有 100 个 test_*.py（测试脚本）文件、645 个测试函数；正式入口不会自动导入它们。 |
| 肉鸽用例 | 5 个文件共 59 项；迁移前在当前工作树通过，耗时 77.858 秒。原文件保留。 |
| 截图 | 引用 62 个不同编号。编号 `20260921-105750-804` 的两份来源内容一致，只收录一份。 |
| 环境 | Python 3.10.19（解释器版本）；本机的 av 17.0.1（视频依赖版本）与旧依赖文件的 10.0.0 不同。测试专用清单单独保存，不改生产依赖。 |
| 覆盖率 | 迁移前没有测量结果；统一入口首次测得的结果作为整理后基线。此前不能推称迁移前百分比。 |

正式目录为 tests（版本化测试），原 test（本机历史测试）保持忽略且不删除。`tests/inventory.json`（分类清单）逐项记录来源、业务、类别、设备需求、断言、样本、状态及未纳入原因；`tests/fixtures/dimensional_exploration/manifest.json`（样本清单）记录 62 个编号、相对路径、服务器、场景、校验值和使用它们的用例。分类清单可用 `dev_tools/build_test_inventory.py`（历史清单生成脚本）从原目录重新生成；样本导入用 `dev_tools/build_exploration_fixtures.py`（样本导入脚本），已有同名目标不会被覆盖。

截图来自游戏过程，入库的是原尺寸图片，原始来源不修改。已查看全部 62 张的分组预览，画面均为肉鸽界面，没有看到账号标识或个人资料；预览图保存在被忽略的 screenshots/fixture_review（样本复核）目录。若以后发现标识，应先在正式副本遮盖、更新校验值，并重新运行识别用例。

## 本机运行

在仓库中安装 `requirements-test-pinned.txt`（固定测试依赖清单）后运行：

```powershell
python -B -m dev_tools.run_offline_tests --list
python -B -m dev_tools.run_offline_tests --coverage --timeout-seconds 300
python -B -m dev_tools.run_offline_tests --suite dimensional_exploration
python -B -m dev_tools.run_offline_tests --case tests.dimensional_exploration.test_dimensional_exploration_shop_new.ShopNewRegressions.test_badges_bind_to_exact_items_in_both_rows
```

入口从自身位置确定当前工作树；从别的工作目录启动时，用该工作树解释器运行入口文件即可。默认运行登记的 59 项肉鸽用例和 4 项运行器契约检查；`--suite dimensional_exploration`（肉鸽测试集）只运行原 59 项。`--list`（列出测试）不执行用例；`--case`（指定用例）使用完整标识；`--coverage`（覆盖率）测量肉鸽非生成业务代码的行和分支，排除资产包装及第三方库。参数、依赖、样本或空集合有问题时退出 2；用例失败或超时退出 1；全部通过且零跳过退出 0。

每次运行在被忽略的 screenshots/offline_test_results（离线测试产物）下创建新目录，不自动删除。`result.json`（结构化结果）包含版本、环境、统计、用例耗时和异常详情；`output.log`（运行日志）保留标准测试输出；失败时复制关联样本至 failures（失败附件）目录。没有截图的规则测试只需要文字详情。样本缺失、校验值变化、尺寸不符、中文识别模型缺失都会在执行前报错。真实设备构造被拦截为测试失败。

## 场景和当前覆盖

| 场景 | 用例情况 |
| --- | --- |
| 未收录徽标与商品位置、预算不足、售出过滤 | 已有商店回归用例覆盖。 |
| 投资、恢复生命、未收录商品购买顺序 | 已有商店回归用例覆盖。 |
| 点击未改变画面、重复帧、余额未更新 | 已有商店与结算回归用例覆盖；完整整局回放仍未覆盖。 |
| 浮窗遮挡时保持商品信息 | 已有回归用例覆盖。 |
| 重复结算和恢复后去重 | 已有结算、事件记录回归用例覆盖。 |
| 国服、国际服中文、国际服英文导入及页面路由 | 一项用例在三个独立子进程中检查；肉鸽识别只声明支持国际服中文。 |
| 网络异常恢复、完整整局、其他业务模块 | 未覆盖，列为后续工作。 |

本轮整理不降低原有 59 项断言、阈值或样本要求。修复新问题时，先保存最小输入和预期行为，再补回归用例；模板识别同时检查正反例，流程修改检查连续帧与动作。不要把真实账号的实机运行放入自动入口。

## 阶段状态与验收记录

| 阶段 | 状态 | 当前证据与待办 |
| --- | --- | --- |
| 迁移前基线 | 已验证 | 59 项原用例通过；100 个脚本、645 个测试函数和 62 个编号已登记。 |
| 样本与公共辅助 | 已验证 | 62 张唯一图片已收录并校验；公共读图、模拟设备和回放辅助已建立；已查看全部样本的分组预览。 |
| 统一入口 | 已验证 | 默认集 63 项连续两次通过，均为 0 失败、0 错误、0 跳过；从另一目录列出相同用例。 |
| 回放与失败报告 | 进行中 | 故意写错断言返回 1，报告保留预期与实际、日志和两张关联截图；误建真实设备返回 1。已记录公共辅助层动作，识别区域和历史用例的全部模拟动作尚未统一写入报告。 |
| 干净环境 | 进行中 | 固定版本已从本机环境记录；用户将执行新环境安装。须在新环境完成安装及两次一致的测试。 |
| 云端自动执行 | 进行中 | 工作流已配置 Windows、Python 3.10.19、5 分钟测试超时、20 分钟任务超时、14 天产物保留，并执行受控负向检查；须取得真实成功记录。 |

当前肉鸽非生成业务代码：行覆盖 933/1106，约 84.4%；分支覆盖 305/450，约 67.8%。`coverage`（覆盖率工具）的合并百分比约 79.6%。已用原 59 项和迁移后的 59 项在同一工作树、同一统计范围各测一次，覆盖行与分支数完全一致，整理未降低覆盖率。这不是全项目覆盖率。

最终验收需同时满足：59 项可追溯且零跳过；62 个截图编号唯一且无个人绝对路径；干净环境两次结果一致；从不同目录指向同一工作树；设备误调用明确失败；缺图、缺模型、错误断言、未知用例和空集合返回非零；失败报告能定位预期与实际及关联样本；GitHub Actions（云端自动化服务）有一次真实成功运行，并验证失败状态及附件。尚未完成的项目不得因工作流文件存在而视作已验证。
