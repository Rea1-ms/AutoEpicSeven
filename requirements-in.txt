# Compatibility placeholder. Direct dependencies are declared only in
# pyproject.toml and resolved versions are recorded only in uv.lock.
# Regenerate requirements.txt with:
# uv export --frozen --no-dev --no-hashes --no-emit-project --no-annotate -o requirements.txt
