# --------------------
# Define default values
# --------------------

# ==================== Alas ====================

Alas:
  Emulator:
    ScreenshotMethod: scrcpy

# ==================== Daily ====================

DataUpdate:
  Scheduler:
    Enable: true
MissionReward:
  Scheduler:
    Enable: true
Mail:
  Scheduler:
    Enable: true
Knights:
  Scheduler:
    Enable: true
PetsGift:
  Scheduler:
    Enable: true
