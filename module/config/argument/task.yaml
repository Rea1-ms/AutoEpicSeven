# --------------------
# Define argument group of tasks.
# --------------------

# ==================== Alas ====================

Alas:
  menu: 'list'
  page: 'setting'
  tasks:
    Alas:
      - Emulator
      - EmulatorInfo
      - Error
      - Optimization
    Restart:
      - Scheduler

# ==================== Daily ====================

Daily:
  menu: 'list'
  page: 'setting'
  tasks:
    MissionReward:
      - Scheduler
      - MissionReward
    Mail:
      - Scheduler
      - Mail
    CommunityAio:
      - Scheduler
      - CommunityAio
    DataUpdate:
      - Scheduler
      - Dashboard
    SanctuaryDaily:
      - Scheduler
    Knights:
      - Scheduler
      - Knights
      - KnightsTeamBattle
    Arena:
      - Scheduler
      - Arena
    Store:
      - Scheduler
      - StoreDaily
      - StoreWeekly
    PetsGift:
      - Scheduler
      - PetsGift
    SecretShop:
      - Scheduler
      - SecretShop
    Combat:
      - Scheduler
      - Combat
      - UrgentTasks
      - CombatRuntime
    Gacha:
      - Scheduler
      - Gacha
      - GachaResult
    Pets:
      - Scheduler
    LimitedActivity:
      - Scheduler
      - LimitedActivity
    SpecialActivity:
      - Scheduler
      - SpecialActivity
      - ActivityRuntime
    DimensionalExploration:
      - Scheduler
      - DimensionalExploration
      - DimensionalExplorationRuntime


# ==================== Tool ====================

Tool:
  menu: 'list'
  page: 'tool'
  tasks:
    CommunityAuth:
      - CommunityAuth

# ==================== Monthly ====================

Monthly:
  menu: 'list'
  page: 'setting'
  tasks:
    SanctuaryMonthly:
      - Scheduler
      - SanctuaryMonthly
